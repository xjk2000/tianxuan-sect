# 阵堂规范

> 本文档由宗主在灵脉勘查时与无上道友共同确定，记录后端开发环境配置和操作规范。

## ⚠️ 重要提醒

**阵堂长老执行命令时必须遵循 [命令安全判断指南](../../docs/guides/command-safety-guide.md)**：

- ✅ 只读命令（`ls`、`cat`、`grep`、`git status`）→ `SafeToAutoRun: true`
- ❌ 写入/安装/启动命令（`npm install`、`mvn package`、`git commit`）→ `SafeToAutoRun: false`

## 项目信息

- **项目名称**: [待填写]
- **项目路径**: [待填写]
- **最后更新**: [待填写]

## 技术栈

### 后端框架
- **框架**: [如 Spring Boot 2.7.x / Spring Boot 3.x / Node.js Express]
- **语言版本**: [如 Java 17 / Node.js 18]
- **构建工具**: [如 Maven 3.8.x / Gradle 7.x / npm]

### 依赖管理
- **包管理器**: [如 Maven / Gradle / npm / pnpm]
- **依赖文件**: [如 pom.xml / build.gradle / package.json]

## 开发环境配置

### 必需环境变量

```bash
# 示例：
export JAVA_HOME=/path/to/java
export MAVEN_HOME=/path/to/maven
export PATH=$JAVA_HOME/bin:$MAVEN_HOME/bin:$PATH

# 数据库连接（如需要）
export DB_HOST=localhost
export DB_PORT=3306
export DB_NAME=database_name
export DB_USER=username
export DB_PASSWORD=password

# Redis 配置（如需要）
export REDIS_HOST=localhost
export REDIS_PORT=6379

# 其他环境变量
[待填写]
```

### 配置文件位置

- **主配置文件**: [如 src/main/resources/application.yml]
- **开发环境配置**: [如 application-dev.yml]
- **测试环境配置**: [如 application-test.yml]
- **生产环境配置**: [如 application-prod.yml]

## 启动命令

### 开发模式启动

```bash
# 进入项目目录
cd [项目路径]

# 启动命令
[待填写，如：mvn spring-boot:run]
[或：./gradlew bootRun]
[或：npm run dev]

# 启动参数（如需要）
[待填写，如：mvn spring-boot:run -Dspring.profiles.active=dev]
```

### 编译构建

```bash
# 清理编译
[待填写，如：mvn clean compile]

# 打包
[待填写，如：mvn clean package]

# 跳过测试打包
[待填写，如：mvn clean package -DskipTests]
```

### 测试命令

```bash
# 运行所有测试
[待填写，如：mvn test]

# 运行单个测试类
[待填写，如：mvn test -Dtest=UserServiceTest]

# 运行单个测试方法
[待填写，如：mvn test -Dtest=UserServiceTest#testCreateUser]
```

## 服务端口

- **主服务端口**: [如 8080]
- **管理端口**: [如 8081]
- **调试端口**: [如 5005]

## API 文档

- **Swagger UI**: [如 http://localhost:8080/swagger-ui.html]
- **API Docs**: [如 http://localhost:8080/v3/api-docs]

## 常用操作

### 数据库迁移

```bash
# Flyway 迁移
[待填写，如：mvn flyway:migrate]

# Liquibase 迁移
[待填写，如：mvn liquibase:update]
```

### 日志查看

```bash
# 日志文件位置
[待填写，如：logs/application.log]

# 实时查看日志
[待填写，如：tail -f logs/application.log]
```

### 热重载配置

- **是否支持热重载**: [是/否]
- **热重载工具**: [如 Spring DevTools / nodemon]
- **热重载配置**: [待填写]

## 代码规范

### 包结构

```
[待填写，如：
src/main/java/com/company/project/
├── controller/     # 控制器层
├── service/        # 业务逻辑层
├── repository/     # 数据访问层
├── entity/         # 实体类
├── dto/            # 数据传输对象
└── config/         # 配置类
]
```

### 命名规范

- **Controller**: [如 UserController]
- **Service**: [如 UserService / UserServiceImpl]
- **Repository**: [如 UserRepository / UserMapper]
- **Entity**: [如 User / UserEntity]
- **DTO**: [如 UserDTO / UserVO]

### 注释规范

- **类注释**: [必需/推荐]
- **方法注释**: [必需/推荐]
- **复杂逻辑注释**: [必需]

## 常见问题

### 问题 1: [待填写]

**现象**: [待填写]

**原因**: [待填写]

**解决方案**: [待填写]

### 问题 2: [待填写]

**现象**: [待填写]

**原因**: [待填写]

**解决方案**: [待填写]

## 注意事项

1. [待填写，如：启动前确保数据库已启动]
2. [待填写，如：首次启动需要执行数据库迁移]
3. [待填写，如：修改配置文件后需要重启服务]

---

**更新记录**:
- [日期] - [更新内容]
